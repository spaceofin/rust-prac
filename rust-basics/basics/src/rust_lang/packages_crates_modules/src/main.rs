use packages_crates_modules::{parent_fn, parent_public};
use packages_crates_modules::public_fn;

fn main() {
    // println!("Hello, Packages, Crates, and Modules!");
    parent_fn();
    parent_public();
    public_fn();
}